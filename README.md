# HTTP Load Tester

A Command Line Tool for load testing http endpoints. You configure the load and get metrics on the endpoint performance in realtime.


## Installation
```bash

```

## Usage
```bash
http_load_tester -l 200k -d 2m
```